---
name: vendor-refresh-log
description: Append-only ledger of /refresh-vendor walks. A walk that finds no drift records only here (no PR). A walk that finds drift records here AND ships a knowledge-file update PR.
status: active
---

# Vendor refresh log

Append-only. Newest entries at the bottom. Each line:

```
YYYY-MM-DD  <vendor>  <result>  <notes>
```

Result codes:

- `no-drift` — walked every claim, none changed. No knowledge-file edit.
- `drift-applied` — drift detected, supervisor approved, knowledge file updated, PR shipped.
- `drift-declined` — drift detected, supervisor rejected (e.g., vendor announcement looked premature). No knowledge-file edit; reason in notes.
- `partial` — could not walk every claim (e.g., docs site down, binary unavailable). Notes which claims were verified.

The log's purpose is **proof that the walk happened** even when no
file changed. A "no-drift" walk is real work — re-verifying claims
takes time — and the ledger preserves the timestamp so the next walk
knows when freshness was last confirmed.

## Entries

```
2026-06-20  codex-cli        drift-applied  Skills §2/§3/§10 + harness reframe. Codex now ships a first-party skills primitive (open Agent Skills standard, .agents/skills/ scan cwd→repo-root, name+description, $skill + description-match invocation). Prior "no first-party skills primitive" claim corrected. Source: developers.openai.com/codex/skills, agentskills.io.
2026-06-20  antigravity-cli  drift-applied  Skills §2 upgraded from TBD/low-confidence to CONFIRMED. Workspace skills load from <workspace-root>/.agents/skills/, global from ~/.gemini/antigravity/skills/; open Agent Skills standard, official Google Codelab. Cross-vendor .agents/skills/ compatibility confirmed. Source: antigravity.google/docs/skills, codelabs.developers.google.com/getting-started-with-antigravity-skills.
2026-07-18  antigravity-cli  drift-applied  Local binary 1.0.14→1.1.4. Installer bootstrap/native-setup flag split + custom-dir behavior corrected; custom/nested agents, hooks paths, built-in guide/dynamic skills, MCP timeout/path fixes, and headless error behavior added. Tier 1 safety correction: dispatcher floor raised to 1.1.4 with no override; requires global strict+sandbox policy, project hardening, --mode plan, recorded write-refusal probe, and inline targets so no read grant is needed. Probe first falsified scratch-local settings (sentinel created in Antigravity default scratch, then removed); after persisting global toolPermission=strict + enableTerminalSandbox=true, headless write_file was explicitly auto-denied and no sentinel existed. Live Tier 1 Antigravity review completed tool-free: no anchored defects; no-anchor suggestions auto-declined per protocol; external claims reconciled against refresh evidence. Headless strict mode loaded but did not honor wildcard or exact-path read_file grants, so both temporary grants were removed and the dispatcher fails closed on permission-denial output. SPA-blocked rate-limit/pricing claims stayed unverified and unchanged. Sources: agy --version/--help/install --help/agent --help v1.1.4, official install.sh, GitHub CHANGELOG + v1.1.4 release, bundled antigravity_guide, live agy --print peer review 2026-07-18.
2026-07-18  claude-code      drift-applied  Binary v2.1.170→v2.1.210. Material: §3/§10 subagent-nesting reversal (v2.1.172 subagents CAN nest, fixed depth 5, 200/session cap) — retired the "compounds with Codex max_depth=1" comparison, pending marker left for next codex-cli walk; §9 permission-mode enum now surfaces `manual` (alias for `default`, v2.1.200). Pricing §7: Max 20x resolved $200 (support.claude.com/11049741; pricing-page fetch still mis-extracts $100), Team + Enterprise added. Additive: §1 auto-memory index enforcement (v2.1.210/.211), §2 /doctor bundled skill (v2.1.205), §3 background-default + /agents wizard removal (v2.1.198), §9 new headless flags + 10-min bg wait cap (v2.1.182) + json-schema hard-error (v2.1.205). Verified-unchanged: §4 hooks (30 events/5 handlers), §2/§3 frontmatter field sets. §6 rate limits NOT re-verified (within VOLATILE 60-day ceiling); §8 add-json binary-vs-docs "stdio or SSE" conflict persists. Sources: code.claude.com/docs (sub-agents, headless, memory, skills, hooks, mcp), claude --help / mcp add-json --help v2.1.210, support.claude.com/11049741, claude.com/pricing. Consistency sweep: also edited codex-cli.md §subagents (L145, L419) to retire the mirror "compounds with Claude Code's subagents-cannot-spawn-subagents" framing (Claude half verified-false here); Codex max_depth=1 left untouched + unverified, codex-cli.md last-verified NOT bumped — pending markers point to the next codex-cli walk.
2026-07-18  model-capabilities  drift-applied  Fable 5 row REWRITTEN — was conflated with /fast (an Opus 4.8/4.7 output-speed toggle); Fable 5 is a distinct Claude-5-family model a tier ABOVE Opus (claude-fable-5, $10/$50, 1M ctx, thinking always-on), Mythos-class (claude-mythos-5, Project Glasswing). /fast now a separate non-model note. Sonnet 4.6→Sonnet 5 (claude-sonnet-5, $3/$15) as current; added model IDs + real $/Mtok; context 200K→1M (except Haiku); pinning example claude-sonnet-4-6→claude-sonnet-5; source→platform.claude.com models overview. Fable access model encoded per @claudeai 2026-07-18 primary source (promo window 07-07→07-12→07-19; from 07-20 Fable standard in Max + Team Premium at 50% of usage limits, Pro/Team Standard usage-credit + $100 one-time credit) — VOLATILE; support/pricing pages did NOT yet reflect it, re-cite next walk. OpenAI §: GPT-5 family→GPT-5.6 Sol/Terra/Luna + 5.5/5.4/mini (folded from codex-cli walk). Motivated the refresh-vendor.md protocol amendment (model-drift extends consistency sweep across cadences), citing this Fable miss. /fast half verified from claude-api Fast Mode quick-ref, NOT from supervisor. Sources: claude-api skill model table (cached 2026-06-24), code.claude.com/docs/en/costs, anthropic.com/news/redeploying-fable-5, support.claude.com.
2026-07-18  codex-cli        drift-applied  Binary 0.139.0 installed, npm latest 0.144.5 (0.140–0.144.5 walked via gh release notes). MAJOR: docs host migrated developers.openai.com/codex/* → learn.chatgpt.com/docs/* (non-1:1 remap) — all 30 citations updated. Material: §3 delegation modes (disabled/explicit-request-only/proactive, Ultra proactive exception, 0.142/0.144); §4 hook-trust model (0.141, --dangerously-bypass-hook-trust, supervised-use tag) + plugin_hooks flag removed; §6 5h ranges updated (model-dependent) + GPT-5.6 Sol/Terra/Luna family. Additive: §2 remote-plugins-default (0.143) + /import-from-Claude-Code + skill_mcp_dependency_install; §5 Bedrock managed auth + encrypted OAuth storage (plaintext default UNCHANGED); §8 MCP tool-search default; §9 exec flags (profile/add-dir/cd/oss/dangerous-bypass) + subcommands (doctor/apply/delete/archive/etc). RESOLVED both pending markers: max_depth=1/max_threads=6/1800s re-verified → rebuilt cross-vendor head-to-head in BOTH files (Codex depth-1-raisable vs Claude depth-5-fixed). §7 pricing left as-is (Pro20x $200 kept; pricing-page renders $100 — same artifact as Claude). GPT-5.6 model drift carried to model-capabilities walk. Verified-unchanged: §1 AGENTS.md native, §4 10 events/hooks key/command-only, §5 plaintext default, §8 mcp-server first-party. Sources: learn.chatgpt.com/docs (subagents, hooks, auth, pricing), gh release view 0.140–0.144.5, codex --help / exec --help / mcp --help / features list 0.139.0.
2026-08-25  claude-code      drift-applied  File last-verified at v2.1.210; local binary v2.1.220; npm latest v2.1.245. Changelog 2.1.211-2.1.245 walked. TWO REVERSALS of claims the file asserted as fact: (1) SS3 subagent nesting -- was "fixed depth 5, not configurable, 200/session cap"; actual default depth 3 AND configurable via CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH (docs history: 5 for v2.1.172-.216 -> 1 for .217-.218 -> 3 from .219), 200-per-session cap REMOVED v2.1.224, replaced by 20-concurrent CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS (.217, ultracode exempt); fork mode default-on v2.1.232 (CLAUDE_CODE_FORK_SUBAGENT). (2) SS6 Agent SDK credit -- the 2026-06-15 monthly credit was ANNOUNCED THEN PAUSED and never took effect; claude -p still draws from subscription usage limits, so scripted exhaustion is 5-hour-window (recovers in hours), not monthly-credit. SS7 credit bullet reverted to match. Also corrected SS2 cross-vendor portable subset 2 fields -> 6 (name/description/license/compatibility/metadata/allowed-tools; non-spec fields hard-error on claude.ai upload). RESOLVED SS8 websocket conflict BY EXECUTION: live probe `claude mcp add-json '{"type":"ws",...}'` accepted ("Added ws MCP server"), control `type:carrierpigeon` rejected -- binary validates the type; add-json help text "stdio or SSE" is a stale label, not behavior. Re-tagged MEDIUM -> STABLE; probe run in isolated scratch cwd and removed. Additive: SS1 /import v2.1.213 (one-time copy, does NOT replace the sync hook) + modified frontmatter stamp + 4MiB CLAUDE.md cap + managed-policy scopes; SS2 frontmatter 16 -> 20 fields (background/metadata/license/compatibility) + boolean spellings + context:fork background default + bundled-skill list + auto-invocation narrowed (/verify,/code-review .215; /deep-research .218); SS3 model defaults to `inherit` recorded (load-bearing for the model-guardrail work); SS4 DirectoryAdded -> 31 events + v2.1.214 hook `if:` single-segment dir/** narrowing + agent-hook workspace trust; SS9 new flag set at .220 + system/init mcp_server_errors + nested subagent stream forwarding + exit drain 30s + -p starting mode `manual`. SECURITY RECORDED, NOT FIXED: a -p session without --bare runs the hooks + MCP config of the directory it is LAUNCHED FROM (not $TARGET -- the script never cd's) with no trust dialog; cross-vendor-review.sh:117 has no --bare. Cross-vendor review (codex 0.144.5, gpt-5.6-sol) returned 3 anchored observations, ALL ACCEPTED AND FIXED: (1) C1 overstated whose hooks run -- reworded to cwd-conditional; (2) the retired head-to-head asserted a replacement ('both vendors now default low') that outran the unverified Codex half -- now retires the comparison with NO replacement claim; (3) post-2.1.220 claims lacked the protocol-required low-confidence marking -- added a Verification-boundary block plus 6 inline low-confidence tags. Dispatcher defects found while running the review (NOT fixed here, own residual): codex exec appends stdin when stdin is not a TTY and the script never closes it, so the codex branch hangs forever in any non-interactive context (fix: < /dev/null); no per-vendor timeout guard; inherits the operator's global reasoning effort (xhigh here). Verified-unchanged: SS1 AGENTS.md not native; SS4 5 handler types; SS5 auth; SS6 5h+weekly structure; SS7 every plan price (pricing-page "From $100" Max-20x mis-extraction PERSISTS, support article authoritative). NOT verified: absolute rate-limit anchors (unpublished by design), issue #34235 status, `agent teams`, and flags added in .221-.245 (local binary is .220). Consistency sweep: model-capabilities.md FACTUAL half applied (Opus 5 row added -- was missing entirely, $5/$25 base with /fast billing at $10/$50; Sonnet 5 $3/$15 -> $2/$10 standard, intro framing dropped; /fast scope 4.8/4.7 -> 5/4.8; 4.8/4.7/Sonnet4.6 marked legacy); model-capabilities.md last-verified deliberately NOT bumped -- OpenAI + Google sections were not re-verified and bumping would reset that file's own 60-day staleness trigger; the vendor-neutral TIER RESTRUCTURE was deferred by supervisor decision to a consolidated pass after the codex + antigravity walks -- dated PENDING marker written into the file. codex-cli.md SS3/SS10 head-to-head RETIRED (Claude half verified-false); Codex numbers left unverified at 0.139.0 vs installed 0.144.5 and codex-cli.md last-verified NOT bumped. Sources: code.claude.com/docs (sub-agents, hooks, skills, memory, mcp, headless, commands, settings-reference), claude --help / mcp --help / ultrareview --help / plugin --help v2.1.220, raw.githubusercontent.com CHANGELOG.md, support.claude.com/15036540 + /11049741, claude.com/pricing, platform.claude.com models overview, live mcp add-json probe 2026-08-25.
```

## Open items carried forward

Residuals surfaced during a walk but deliberately left outside that
walk's approved scope. Future walks pick these up; remove the row when
the item lands.

| Opened | Item | Picked up by |
|---|---|---|
| 2026-07-18 | [`cross-vendor-harness.md`](../meta-layer/cross-vendor-harness.md) "Asymmetry that's NOT going away" prose still reads "Claude Code has 28 hook events" and "Codex has 6" — both stale. The per-vendor-config table in the same file already reads 30 / 10, and the vendor files are authoritative (`claude-code.md` §4 = 30 events; `codex-cli.md` §4 = 10). Fix the prose to match. | follow-up `/doc-consistency` pass |
| 2026-07-18 | Fable 5 access-model claim (Max + Team Premium at 50% of usage limits from 2026-07-20; Pro + Team Standard usage-credit-based with a one-time $100 credit) is cited to the 2026-07-18 @claudeai announcement because `support.claude.com` and `claude.com/pricing` had not yet published it. Re-cite against the stabler support/pricing URL once they catch up. | next `/refresh-vendor model-capabilities` |
| 2026-08-25 | `scripts/cross-vendor-review.sh:117` runs `claude -p` with **no `--bare`**, which per current docs executes the `.claude/settings.json` hooks and connects the `.mcp.json` servers **of the directory the dispatcher is launched from** (the script passes `$TARGET` as prompt text and never `cd`s, so it is cwd config, not `$TARGET` config) with no trust dialog and no per-server approval. Conditional exposure: it bites when the dispatcher runs from inside an untrusted checkout. The `--allowedTools` allowlist constrains Claude's tools, not that directory's hooks. (Wording corrected after Codex cross-vendor review flagged the original as overstated.) Fix is not one line: `--bare` forces `ANTHROPIC_API_KEY`/`apiKeyHelper` auth. Recorded in `claude-code.md` §9. | dedicated Tier 1 PR |
| 2026-08-25 | Cross-vendor head-to-head on subagent nesting is retired in both `claude-code.md` §3/§10 and `codex-cli.md` §3/§10 and not yet rebuilt. Codex half unverified: file records `max_depth=1` / `max_threads=6` at binary 0.139.0; installed binary is 0.144.5. | next `/refresh-vendor codex` |
| 2026-08-25 | `model-capabilities.md` heuristics table is Claude-shaped (names `Sonnet-tier`/`Haiku-tier`/`Opus-tier`, unresolvable on peer vendors) and has **no search / retrieval / fan-out row** — the task shape most likely to inherit an expensive model. OpenAI cost cells read `varies`; the Google table has one row and no models (`agy models` lists 14 live). Restructure to vendor-neutral tiers + per-vendor mapping. | consolidated pass after codex + antigravity walks |
| 2026-08-25 | No agent definitions exist for ANY vendor (`.claude/agents/`, `.codex/`, `~/.gemini/antigravity/` all absent) and no `model:` is pinned anywhere, so every Claude subagent inherits the session model (`opus[1m]`, effort `high`) — including the built-in `Explore` fan-out agent. Per-agent model pinning is verified available on Claude Code; **unverified** on Codex (`.codex/agents/*.toml` documents only `name`/`description`/`developer_instructions`) and Antigravity. | Phase 0 probes folded into the codex + antigravity walks |
| 2026-08-25 | `antigravity-cli.md` records binary 1.1.4; installed is **1.1.19** (15 patch versions). Not walked this session. | next `/refresh-vendor antigravity` |
| 2026-08-25 | `scripts/cross-vendor-review.sh` codex branch hangs forever in non-interactive use: `codex exec` appends stdin to the prompt when stdin is not a TTY, and the script never closes it (`Reading additional input from stdin...`). Works interactively, hangs in CI/background/piped. Fix: `< /dev/null`. Also: no per-vendor timeout guard, and the invocation inherits the operator's global `model_reasoning_effort` (xhigh on this machine), which alone pushed a 553-line diff review past 9 minutes. | same Tier 1 PR as the `--bare` fix |
| 2026-08-25 | `scripts/check-skill-frontmatter.sh` enforces a two-field portable subset (`name` + `description`) while the Agent Skills spec allows six (`+ license, compatibility, metadata, allowed-tools`). Being stricter is defensible policy, but the script's rationale comment calls `allowed-tools` "Claude Code specific", which is factually wrong — it is a spec field. Decide: keep the strict floor (fix the comment) or widen the gate to the spec's six. | supervisor decision |
